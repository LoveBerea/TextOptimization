# 2026-04-20 工作日志

## 完成文本优化酒馆助手脚本项目

创建了完整的酒馆助手脚本，位于 `src/文本优化/` 目录下，包含6个文件：

1. **settings.ts** - AI配置管理（zod + pinia），支持3种配置槽位，4种AI格式预设（openai/deepseek/gemini/kimi）
2. **optimizer.ts** - 核心逻辑：正则提取文本、AI调用优化、替换写回楼层
3. **配置界面.vue** - 配置面板，挂载到酒馆扩展设置区
4. **对比界面.vue** - 原文vs优化文本对比浮层，确认/取消操作
5. **index.ts** - 主入口，4个按钮注册（优化/配置/确认/取消），Vue组件挂载
6. **state.ts** - 共享响应式状态，解决 index.ts 与 对比界面.vue 的循环依赖

同时在 `脚本/导入到酒馆中/` 下创建了导入JSON文件。

## 提示词双套功能与JSON修复

- `settings.ts`：将单个 `prompt` 字段替换为 `prompt1`/`prompt2`/`active_prompt`，提供两套默认提示词
  - prompt1：风格润色（保留原意、修正措辞、提升节奏感）
  - prompt2：创意扩写（丰富细节/氛围感，扩写约1.5~2倍）
- `optimizer.ts`：`optimizeText()` 新增 `prompt: string` 参数，由调用方传入（不再从 config 内读取）
- `index.ts`：调用 `optimizeText` 时传入 `store.activePrompt`
- `配置界面.vue`：增加双提示词编辑区，每套有标题、textarea 和启用/使用中徽章
- `脚本-文本优化.json`：修复为正确格式，补充 `type/enabled/button` 字段，buttons 列表为优化/配置（确认/取消移至对比界面内部）
- `index.ts`：删除 `handleConfirm`/`handleCancel` 及 `applyOptimization` 导入，只保留"优化"和"配置"两个外部按钮
- `对比界面.vue`：修复 `onRetry` 调用 `optimizeText` 时缺少 `prompt` 参数的问题
- CSS MIME 报错 `table.css`：非本脚本引起，是酒馆自身的 404 样式引用

## GitHub Actions Node 20 弃用修复

- `bundle.yaml`：删除 `JesseTG/rm@v1.0.3`（Node 20 action），改用原生 `rm -rf dist`；`pnpm/action-setup@v4` 升级到 `@v4.1.0`
- `bump_deps.yaml`：`actions/setup-node@v4` → `@v6`，node-version 22→24；`pnpm/action-setup@v4` → `@v4.1.0`
- `sync_template.yaml`：无变更，`actions/checkout@v4` 本身兼容 Node 24 runtime

## GitHub Actions Node 20 弃用修复（第二轮）

- 三个工作流全部升级 `actions/checkout@v4` → `@v5`（v5 原生 Node 24 runtime）
- 三个工作流全部添加 `env: FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true`，覆盖 `pnpm/action-setup@v4.1.0`、`EndBug/add-and-commit@v9.1.3`、`AndreasAugustin/actions-template-sync@v2`、`phish108/autotag-action@v1.1.64` 等尚未发布 Node 24 版本的第三方 action

